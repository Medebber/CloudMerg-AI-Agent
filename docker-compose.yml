version: '3.9'

services:
  app:
    build: .
    image: cloudmerg-chatbot
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
